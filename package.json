{
  "name": "hono-form-with-firestore-example",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler build --env production",
    "deploy": "wrangler deploy --minify"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "nanoid": "^5.0.9",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "wrangler": "^3.96.0"
  }
}
